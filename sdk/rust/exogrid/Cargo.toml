[package]
name = "exogrid"
version = "1.0.0"
edition = "2021"
description = "Safe Rust bindings for ExoGridChart"
authors = ["ExoGridChart"]
license = "MIT"

[dependencies]
exogrid-sys = { path = "../exogrid-sys" }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
